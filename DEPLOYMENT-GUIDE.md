# 🚀 GitHub Pages 一键部署指南

## 📋 概述

这个部署脚本可以让您的 Next.js 博客一键部署到 GitHub Pages，无需修改任何现有代码。

## ✨ 特性

- 🔄 **零代码修改** - 完全不触碰现有代码
- 🎯 **一键部署** - 单命令完成所有操作
- 🧠 **智能检测** - 自动识别项目配置
- 🛡️ **错误恢复** - 失败时自动回滚
- 🌍 **跨平台** - 支持 Windows、macOS、Linux

## 🚀 快速开始

### 1. 使用部署脚本

```bash
# 方法1: 直接运行脚本
node deploy.js

# 方法2: 使用 npm 脚本
npm run deploy:gh-pages
```

### 2. 部署流程

脚本会自动执行以下步骤：

1. 📋 **备份配置** - 备份原始的 `next.config.ts`
2. ⚙️ **创建静态配置** - 临时生成适合 GitHub Pages 的配置
3. 🔧 **添加静态参数** - 自动为动态路由添加 `generateStaticParams()`
4. 🔨 **构建站点** - 构建静态文件到 `out` 目录
5. 🚀 **部署到 GitHub** - 推送到 `gh-pages` 分支
6. 🧹 **清理恢复** - 删除临时文件，恢复原始配置

## 📁 脚本功能详解

### 自动检测功能

- **仓库名称** - 自动从 git remote 获取
- **项目类型** - 检测是否为 Next.js 项目
- **Git 状态** - 检查是否有未提交的更改
- **配置文件** - 自动备份和恢复
- **动态路由** - 自动为静态导出添加 `generateStaticParams()`

### 错误处理

- ✅ 构建失败时自动恢复配置
- ✅ 部署失败时清理临时文件
- ✅ 网络错误时提供友好提示
- ✅ 权限错误时给出解决方案

## 🔧 配置说明

### 自动生成的配置

脚本会临时创建以下配置：

```typescript
// next.config.ts (临时)
const nextConfig = {
  output: 'export',           // 静态导出
  trailingSlash: true,         // 尾部斜杠
  images: {
    unoptimized: true,         // 禁用图片优化
  },
  basePath: '/your-repo-name', // 基础路径
  assetPrefix: '/your-repo-name', // 资源前缀
  distDir: 'out',              // 输出目录
}
```

### 支持的文件

- ✅ `.nojekyll` - 禁用 Jekyll 处理
- ✅ `CNAME` - 自定义域名支持
- ✅ 所有静态资源

## 🌐 访问地址

部署完成后，您的博客可以通过以下地址访问：

```
https://your-username.github.io/your-repo-name
```

### 自定义域名

如果您有自定义域名，请在项目根目录创建 `CNAME` 文件：

```
your-domain.com
```

## 📝 使用前准备

### 1. 确保项目是 Git 仓库

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/your-repo.git
git push -u origin main
```

### 2. 启用 GitHub Pages

1. 进入 GitHub 仓库设置
2. 找到 "Pages" 选项
3. 选择 "gh-pages" 分支
4. 保存设置

### 3. 确保代码已提交

脚本会检查是否有未提交的更改，如有请先提交：

```bash
git add .
git commit -m "Ready for deployment"
git push
```

## 🛠️ 故障排除

### 常见问题

#### 1. "检测到未提交的更改"
```bash
git add .
git commit -m "Ready for deployment"
git push
```

#### 2. "当前目录不是git仓库"
```bash
git init
git remote add origin https://github.com/username/your-repo.git
```

#### 3. "构建失败"
- 检查 `package.json` 中的依赖
- 运行 `npm install` 安装依赖
- 检查代码是否有语法错误

#### 4. "部署失败"
- 检查 GitHub 仓库权限
- 确认分支名称正确
- 检查网络连接

### 调试模式

如果遇到问题，可以查看详细日志：

```bash
node deploy.js
```

脚本会显示每个步骤的详细信息。

## 🔄 手动部署（备选方案）

如果自动脚本有问题，您也可以手动部署：

```bash
# 1. 备份配置
cp next.config.ts next.config.ts.backup

# 2. 创建静态配置
# (参考上面的配置示例)

# 3. 构建项目
npm run build

# 4. 部署到 GitHub Pages
npm run deploy

# 5. 恢复配置
mv next.config.ts.backup next.config.ts
```

## 📚 相关文档

- [Next.js 静态导出](https://nextjs.org/docs/advanced-features/static-html-export)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
- [MDX 支持](https://mdxjs.com/)

## 🤝 贡献

如果您发现脚本有问题或有改进建议，请：

1. 提交 Issue 描述问题
2. 提交 Pull Request 改进脚本
3. 分享使用经验

---

**注意**: 这个脚本专门为您的 Next.js 博客项目设计，确保在项目根目录运行。
