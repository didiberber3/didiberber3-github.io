## 标题

# 一级标题 h1

## 二级标题 h2

### 三级标题 h3

#### 四级标题 h4

---

## 正文

这是一段普通正文。本站的排版风格强调可读性：大行高、舒适字距、清晰的层级。汉字与英文混排时，在排版上也保持协调。

这是一段较长的正文，用来测试阅读体验。本站采用 1.8 的行高和 0.01em 的字距，确保长文阅读不疲劳。段落之间保留足够的间距，让内容结构一目了然。

---

## 引用

> 这是一段块引用。引用块使用绿色左边框作为视觉标记，与正文清晰区分。
>
> 引用可以包含多段内容，每段前都有引用标记。

---

## 列表

### 无序列表

- 列表项一
- 列表项二
- 列表项三
  - 嵌套列表项 A
  - 嵌套列表项 B

### 有序列表

1. 第一步
2. 第二步
3. 第三步

---

## 代码

### 行内代码

使用 `console.log()` 打印调试信息。使用 `Array.from()` 转换类数组对象。

### 代码块

```java
public class HelloWorld {
    public static void main(String[] args) {
        // 打印输出
        System.out.println("Hello, World!");
    }
}
```

更多代码示例：

```bash
# 查看日志
tail -f /var/log/nginx/access.log | grep 404
```

```powershell
# PowerShell 脚本
Get-ChildItem -Path "C:\logs" -Recurse -Filter "*.log" | ForEach-Object {
    Write-Output $_.FullName
}
```

---

## 强调

- **粗体文字**用于强调重要概念
- *斜体文字*用于轻微强调
- ~~删除线~~表示废弃内容
- `行内代码` 用于技术术语

---

## 分隔线

段落一

---

段落二

---

## 表格

| 特性 | 支持 | 说明 |
|------|------|------|
| 圆角 | ❌ | 全站无圆角 |
| 暗色模式 | ✅ | 支持切换 |
| 懒加载 | ✅ | 按需加载 MD |
| 搜索 | ✅ | 标题搜索 |
